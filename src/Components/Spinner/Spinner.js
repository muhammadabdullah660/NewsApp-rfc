import React, { Component } from "react";
import loading from "../../Resources/loading.gif";
const Spinner = () => {
  return (
    <div>
      <img src={loading} alt="" />
    </div>
  );
};

export default Spinner;
